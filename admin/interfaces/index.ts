// product details interface
export interface ProductInfo {
    _id: string;
    name: string;
    description: string;
    price: number;
    images: string[];
    category: string;
    dressStyle: string;
}
